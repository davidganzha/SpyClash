# Подтверждённое обновление сервера — 15 сентября 2026

**Результат: шесть функций успешно обновлены; проверка обратной выгрузки пройдена.**

Пользователь подтвердил точную операцию сообщением «подтверждаю» после перечня шести функций и ссылки на проверенный пакет Build 157. Это подтверждение использовано только для этой серверной операции.

## Объект и пакет

- App ID: `69a0e57fa939f578082f8091` (SpyClash).
- Source commit: `d487470d050debd452679a8e3f1b850eb993e90f`.
- Документированный пакет: `41b36700228c14299c6c4841ca1c05f1a0198842`.
- SHA-256 `production.patch`: `ef0587f4625f686010387cd04c5860f0497c15579c15c09fdbb07f5bbad1bde7`.
- Stage: `/tmp/spyclash-409-157-deploy`.
- CLI: `base44@0.1.4`, явный `--app-id`, named `functions deploy`; generic deploy и force не использовались.
- Функции: `communityAction`, `gameRoomAction`, `generateWordPack`, `notificationAction`, `pushNotificationAction`, `wordPackAction`.

## Доказательства

1. Авторизация Base44 перепроверена перед CLI-операциями.
2. Свежая выгрузка в `2026-09-15T15:12:35.300315+00:00`: все 17 функций / 194 runtime-файла совпали с утверждённой исходной базой; отклонений не найдено.
3. Локальный stage независимо проверен: 128 runtime-файлов, все хеши совпали с manifest; 22 изменённых/добавленных файла; 213 относительных импортов разрешаются внутри собственных bundles.
4. CLI завершился с кодом 0 и итогом `6 deployed`.
5. Обратная выгрузка в `2026-09-15T15:15:35.159335+00:00`: 17 функций / 197 runtime-файлов. Все 128 файлов целевых функций совпали с утверждённым пакетом. Остальные 11 функций / 69 runtime-файлов сохранены.
6. Все 17 конфигураций совпали с JIT-выгрузкой, включая существующее выключенное расписание Push drain. Дополнительное поле automations в Push является сохранённой исходной настройкой, а не новым изменением.
7. Независимая проверка подтвердила те же результаты, отсутствие неожиданных файлов/пропусков/дубликатов и сохранность существующего `appleAuthCallback/headers_test.ts`, не входящего в runtime-счётчик.
8. Свежий запрос логов шести функций с момента preflight вернул пустой результат. Это не является проверкой реального игрового трафика.

Путь `base44/functions/<function>/entry.ts` в выгрузке нормализован к логическому `main.ts`; содержимое каждого файла проверено по SHA-256. Неизвестные пути и дубликаты не принимались.

## Границы выполненного

Схемы данных, секреты, другие функции, публикация сайта, TestFlight и App Store не изменялись. Клиентские изменения iOS/Web сохранены в GitHub, но этой операцией не установлены и не опубликованы. Полная проверка двух авторизованных клиентов после обновления пока не проведена. Правомерные конфликты состояния могут по-прежнему возвращать 409.

## Файлы доказательств

- `deployment-preflight.json` — свежий исходный инвентарь и конфигурации.
- `deployment-postflight.json` — итоговые хеши и сравнение области изменений.
- `deployment.log` — результаты шести named deployment.
- `postdeploy-logs.txt` — результат чтения логов.
